* fix MIME type obtention in g_mk_p().
    we need to calculate it by ourselves since $q->filename makes apache lazy.
* Include script to generate the front page and feeds
* remove g_suri, g_location; 
    make a module which inherits from Apache::Request
    (which inherits from Apache)
* Support long names in the menu (currently they break up ugly)
* Load (and parse) configuration only on Server load
* Improve the tools/ scripts
    Add config generator for apache2
		Parse command line options
* support a set of macros into markdown... and document them.
